# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.12

# Validation & settings
pydantic>=2.8.0
pydantic-settings>=2.4.0

# Database (relational)
sqlalchemy>=2.0.0
alembic>=1.14.0
asyncpg>=0.30.0

# Vector database
qdrant-client[fastembed]>=1.13.0

# RAG orchestration
langgraph>=0.3.0

# MCP server (exposes all FastAPI routes as Cursor-callable tools)
fastapi-mcp>=0.4.0

# RAG evaluation metrics (run with pytest -m eval inside Docker)
ragas>=0.4.3

# Groq cloud LLM inference (fast LPU-backed API)
langchain-groq>=0.2.0
# Ollama kept as local fallback when GROQ_API_KEY is not set
langchain-ollama>=0.3.0
# Required by ragas 0.4.x for LLM provider integrations
langchain-community>=0.4.0

# PDF processing
pypdf>=5.0.0

# Async HTTP
httpx>=0.28.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
coverage>=7.6.0
aiosqlite>=0.20.0
